apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: cloudpredict
  labels:
    app: cloudpredict
    component: redis
spec:
  type: ClusterIP
  selector:
    app: cloudpredict
    component: redis
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
